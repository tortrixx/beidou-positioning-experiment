# Sample Dataset

This folder contains the default small-entry sample used by the CLI, GUI, and tests.

Files:

- `bjfs1170.26o`
- `brdc1170.26n`

The same source files are also managed as the full dataset `data/datasets/bjfs_2026_117_gps/`.
